<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id.chendradewangga_00000029610_if633_el_uts">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChendraDewangga_00000029610_IF633_EL_UTS">
        <activity android:name=".MusicList" android:label="Music List" android:parentActivityName=".LoginPage"></activity>
        <activity android:name=".MyProfile" android:label="My Profile" android:parentActivityName=".MainActivity"/>
        <activity android:name=".LoginPage" android:label="Login" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MusicPlayer" android:label="Media Player" android:parentActivityName=".MusicList"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>